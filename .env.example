# ============================================================
# MyCoach — Environment Variables
# ============================================================
# Copy to .env and fill in your values
#   cp .env.example .env

# ── Security ─────────────────────────────────────────────────
# Generate with: openssl rand -hex 32
SECRET_KEY=change-me-in-production

# ── App ──────────────────────────────────────────────────────
PORT=8000
TZ=Europe/Paris

# ── Google Calendar OAuth ─────────────────────────────────────
# console.cloud.google.com → APIs & Services → Credentials → OAuth 2.0 Client ID
# Enable the Google Calendar API in your project first!
# Authorized redirect URI to add: http://YOUR_IP:8000/auth/google-calendar/callback
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Public URL of your MyCoach instance (used for OAuth redirect URI)
MYCOACH_URL=http://localhost:8000
